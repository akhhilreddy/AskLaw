import { useState } from "react";
import { Copy,Check } from "lucide-react";
export default function CodeBlock({language,children}){const [copied,setCopied]=useState(false);const copy=async()=>{try{await navigator.clipboard.writeText(children);setCopied(true);setTimeout(()=>setCopied(false),2000)}catch(error){console.error("Could not copy code:",error)}};return <div style={{border:'1px solid #e0e4e8',borderRadius:9,overflow:'hidden',margin:'16px 0'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',background:'#edf0f3',padding:'7px 12px',fontSize:11,color:'#687888'}}><span>{language}</span><button type="button" onClick={copy} style={{display:'inline-flex',gap:5,alignItems:'center',border:0,background:'transparent',color:'inherit'}}>{copied?<Check size={13}/>:<Copy size={13}/>} {copied?"Copied":"Copy"}</button></div><pre style={{margin:0,padding:16,background:"#f8f9fa",fontSize:12,overflow:"auto"}}><code>{children}</code></pre></div>}
