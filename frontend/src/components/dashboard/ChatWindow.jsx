import { useEffect,useRef } from "react";
import ChatMessage from "./ChatMessage";
export default function ChatWindow({messages,isTyping}){const endRef=useRef(null);useEffect(()=>{endRef.current?.scrollIntoView({behavior:"smooth",block:"end"})},[messages,isTyping]);return <div className="chat-scroll" role="log" aria-label="Research conversation" aria-live="polite"><div className="chat-thread">{messages.map(message=><ChatMessage key={message.id} message={message}/>)}{isTyping&&<div className="chat-entry"><div className="answer-header"><span className="answer-mark">A</span> AskLAW</div><div className="streaming-line"><span className="pulse-dot"/> Researching…</div></div>}<div ref={endRef}/></div></div>}
